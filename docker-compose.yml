services:
  api:
    build: .
    image: my-hd-pipeline:latest
    environment:
      - NODE_ENV=production
      - API_KEY=secret123        # keep smoke-test happy
    ports:
      - "3000:3000"
    volumes:
      - ./db.json:/app/db.json:ro
